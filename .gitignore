# ── Scratch / temp files ──────────────────────────────────────────
Project_architecture_document_exemple.pdf
test_samples.py
Diagram.jpeg

# ── Dependencies ──────────────────────────────────────────────────
node_modules/

# ── Build output ──────────────────────────────────────────────────
dist/
build/

# ── Environment variables (never commit secrets!) ─────────────────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── Logs ──────────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── OS files ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDE ───────────────────────────────────────────────────────────
.vscode/
.idea/

# ── Claude Code (contains secrets — never commit) ─────────────────
.claude/
.agents/
skills-lock.json

# ── AI model weights (large binary files — use HuggingFace Hub) ───
*.pth
*.pt
*.onnx
*.bin
*.safetensors

# ── Python ────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.pytest_cache/
venv/
.venv/

# ── Design mockups (reference only, not source code) ──────────────
Frontend/stitch/

# ── Training data (large binary files — store locally or on cloud) ─
Products-raw-photos/
*.zip
AIService/raw-photos/
scripts/data-prep/raw-photos/

# ── Architecture docs (screenshots + helper files, not source) ──────
Management/Architecture/web_screenshots/
Management/Architecture/project_entry_diagram.png
Management/Architecture/architecture_diagram.svg
Management/Architecture/architecture_update.md

# ── Word temp/lock files ─────────────────────────────────────────────
~$*.docx
~$*.doc
~WRL*.tmp
